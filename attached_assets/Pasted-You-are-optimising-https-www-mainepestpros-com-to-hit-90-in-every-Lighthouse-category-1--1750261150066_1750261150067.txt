You are optimising https://www.mainepestpros.com/ to hit ≥90 in every Lighthouse category.

1. 🔎  BASELINE
   • Run `npx lhci autorun` on / and /services pages; save JSON to /lighthouse/before/.

2. ⚡  CRITICAL PATH
   • Use the `critters` or `critical` npm package to inline above-the-fold CSS; move the rest to /assets/styles.[hash].css loaded with `media="print" onload="this.media='all'"`.
   • Remove `loading="lazy"` from the LCP hero image (above-the-fold) and mark it with `<link rel="preload" as="image">`.

3. 🖼  IMAGE OPTIMISATION
   • Convert all PNG/JPEG >50 KB to WebP; update `<img>` with `/.netlify/images?url=` variants & add `w=`, `q=` params.
   • Keep hero ≤150 KB and ≤1920 px wide.
   • Add `loading="lazy" decoding="async"` to **non-LCP** images.

4. 🪄  JS / CSS SHRINK
   • Enable Vite/React dynamic `import()` for route-level splitting.
   • Tree-shake dependencies; purge unused CSS with purgeCSS/tailwind jit.

5. 🔧  NETLIFY BUILD PLUGINS
   • Create/modify `netlify.toml`:
     [build]
       command = "npm run build"
       publish  = "dist"

     [[plugins]]
       package = "netlify-plugin-minify-html"

     [[plugins]]
       package = "netlify-plugin-inline-critical-css"

6. 📜  HEADERS & CACHING
   • Add `_headers`:
     /assets/*
       Cache-Control: public, max-age=31536000, immutable

   • Add `_redirects`:
     /*  https://www.mainepestpros.com/:s 301!

   • Add HSTS in `_headers`:
     /*
       Strict-Transport-Security: max-age=63072000; includeSubDomains; preload

7. 🔤  FONT PERFORMANCE
   • Self-host Google Fonts, add `font-display:swap`.
   • Preload primary `.woff2` with `<link rel="preload" as="font" crossorigin>`.

8. ♿  ACCESSIBILITY
   • Auto-scan for missing `alt`; inject descriptive alt text.
   • Ensure color contrast ≥ 4.5:1; adjust CSS vars if needed.
   • Add `aria-label` to all icon-only buttons.

9. 🛡️  BEST PRACTICES
   • Replace any `http://` resources with `https://`.
   • Add `rel="noopener"` to external links with `target="_blank"`.
   • Remove deprecated APIs flagged by Lighthouse.

10. 🧪  POST-BUILD QA
    • Re-run `lhci autorun`; compare against baseline. If any score < 90, abort (`exit 1`).