1 – Replace & prioritise the hero
html
Copy
Edit
<link rel="preload" as="image"
      href="/images/maine-pest-pros-hero.avif"
      imagesrcset="/images/maine-pest-pros-hero.avif 1600w">
<img src="/images/maine-pest-pros-hero.avif"
     width="1600" height="960"
     fetchpriority="high" decoding="async">
fetchpriority="high" is now the recommended LCP booster 
web.dev
.

2 – Serve every other image via Netlify Image CDN
/.netlify/images?url=/images/logo.png&w=256&q=80 gives responsive WebP/AVIF on-the-fly 
github.com
.

3 – Inline critical CSS with Critters
npx critters dist --minify (faster & more reliable than the Netlify plugin on Vite apps) 
netlify.com
reddit.com
.

4 – Split the bundle correctly
js
Copy
Edit
// vite.config.js
output: { manualChunks: { react:['react','react-dom'], vendor:['@heroicons/react'] } }
Fixing the key name lets Rollup make 30–40 KB vendor chunks 
github.com
.

5 – Async Google Fonts
Self-host or keep the CSS but add &display=swap and <link rel="preconnect" …> to kill render-blocking ﻿
stackoverflow.com
.

6 – Give every image width/height
Prevents the two big layout shifts Lighthouse pin-pointed 
debugbear.com
.

One-shot Replit-agent prompt
text
Copy
Edit
You are rehabbing https://www.mainepestpros.com for ≥90 Lighthouse Performance.

1.  baseline: `npx lhci autorun` > /lhc/before.json

2.  HERO
    • convert hero-pests.png → 1600 px AVIF ≤80 KB
    • remove lazy attr, add preload tag (as=image) and fetchpriority="high"
    • delete old PNG

3.  IMAGES
    • replace every <img src="/images/..."> with Netlify CDN URLs  (/.netlify/images?url=)
    • add width & height attrs to all images
    • netlify.toml →   [images] remote_images=[]  # allow CDN

4.  CRITICAL CSS
    • `npm i -D critters`
    • `npx critters dist --minify`
    • netlify.toml: keep only netlify-plugin-minify-html

5.  GOOGLE FONTS
    • change CSS href to `...css2?family=Inter:wght@400;600;700&display=swap`
    • add `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>`

6.  JS SPLIT
    • vite.config.js → correct `manualChunks` as shown; switch route files to dynamic import()
    • verify main.*.js < 80 KB

7.  HEADERS & HSTS
    _headers:
      /assets/*  Cache-Control: public,max-age=31536000,immutable
      /*         Strict-Transport-Security: max-age=63072000;includeSubDomains;preload
    _redirects:
      /*  https://www.mainepestpros.com/:s 301!

8.  post-build: `npx lhci autorun` > /lhc/after.json; abort if Performance < 90
